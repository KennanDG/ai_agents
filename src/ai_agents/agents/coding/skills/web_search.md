# Web Search Skill

Purpose: Perform a web search and return the top result.

Instructions:
- Use the `search` function from the `agents.coding.tools.web_search_tool` module.
- Provide a concise summary of the search results.
- Handle empty or error responses gracefully.
